import * as React from 'react'
import Input from './Input'

const Filters: React.FC = () => {
  return (
    <React.StrictMode>
      <div>
        <div>
          <Input />
        </div>
      </div>
    </React.StrictMode>
  )
}

export default Filters
